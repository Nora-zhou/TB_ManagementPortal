---
mode: agent
description: Initialize a git repository with standard configuration
---

Initialize a git repo, create a .gitignore, set up the main branch, and make an initial commit with the current project files.
